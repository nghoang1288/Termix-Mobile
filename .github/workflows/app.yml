name: Build and Release SSHBridge Mobile
on:
  workflow_dispatch:
    inputs:
      platform:
        type: choice
        description: Platform to build for
        default: "both"
        required: true
        options:
          - both
          - ios
          - android
      action:
        type: choice
        description: What to do with the built app
        default: "file"
        required: true
        options:
          - none
          - file
          - release
          - submit

permissions:
  contents: write

jobs:
  build:
    name: build-${{ matrix.platform }}
    runs-on: ${{ matrix.platform == 'ios' && 'macos-26' || 'blacksmith-8vcpu-ubuntu-2404' }}
    strategy:
      matrix:
        platform: ${{ github.event.inputs.platform == 'both' && fromJSON('["ios", "android"]') || fromJSON(format('["{0}"]', github.event.inputs.platform)) }}
        node: [20.x]
    steps:
      - name: Setup repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4.0.2
        with:
          node-version: ${{ matrix.node }}
          cache: "npm"

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v7
        with:
          token: ${{ secrets.EXPO_TOKEN }}
          expo-version: latest
          eas-version: latest

      - name: Install dependencies
        run: npm install

      - name: Validate EAS configuration
        run: |
          echo "EAS configuration:"
          cat eas.json
          echo "Validating configuration..."
          eas build:configure --platform ${{ matrix.platform }}

      - name: Build app
        run: |
          if [ "${{ github.event.inputs.action }}" = "submit" ]; then
            PROFILE="production"
          else
            PROFILE="preview"
          fi

          echo "Building for platform: ${{ matrix.platform }}"
          echo "Using profile: $PROFILE"

          mkdir -p build-artifacts

          VERSION=$(node -p "require('./app.json').expo.version")
          echo "App version: $VERSION"

          eas build --local \
            --non-interactive \
            --clear-cache \
            --platform=${{ matrix.platform }} \
            --profile=$PROFILE

          if [ "${{ matrix.platform }}" = "android" ]; then
            echo "Searching for Android build artifacts..."

            APK_FILE=$(find . -maxdepth 1 -type f -name "build-*.apk" 2>/dev/null | head -1)

            if [ -n "$APK_FILE" ]; then
              cp "$APK_FILE" "build-artifacts/sshbridge_android.apk"
              echo "Copied to build-artifacts/sshbridge_android.apk"
            else
              echo "Trying /tmp search..."
              APK_FILE=$(find /tmp -type f -name "app-release.apk" 2>/dev/null | grep "eas-build-local-nodejs" | head -1)

              if [ -n "$APK_FILE" ]; then
                echo "Found APK in /tmp: $APK_FILE"
                cp "$APK_FILE" "build-artifacts/sshbridge_android.apk"
              else
                echo "ERROR: APK not found after build"
                ls -la . | grep -E '\.apk|build-'
              fi
            fi

            if [ "$PROFILE" = "production" ]; then
              AAB_FILE=$(find . -maxdepth 1 -type f -name "build-*.aab" 2>/dev/null | head -1)

              if [ -n "$AAB_FILE" ]; then
                cp "$AAB_FILE" "build-artifacts/sshbridge_android.aab"
              else
                AAB_FILE=$(find /tmp -type f -name "app-release.aab" 2>/dev/null | grep "eas-build-local-nodejs" | head -1)
                if [ -n "$AAB_FILE" ]; then
                  echo "Found AAB in /tmp: $AAB_FILE"
                  cp "$AAB_FILE" "build-artifacts/sshbridge_android.aab"
                fi
              fi
            fi
          elif [ "${{ matrix.platform }}" = "ios" ]; then
            echo "Searching for iOS build artifacts..."

            IPA_FILE=$(find . -maxdepth 1 -type f -name "build-*.ipa" 2>/dev/null | head -1)

            if [ -n "$IPA_FILE" ]; then
              echo "Found IPA in project root: $IPA_FILE"
              cp "$IPA_FILE" "build-artifacts/sshbridge_ios.ipa"
              echo "Copied to build-artifacts/sshbridge_ios.ipa"
            else
              IPA_FILE=$(find . -type f -name "*.ipa" -not -path "*/node_modules/*" 2>/dev/null | head -1)

              if [ -n "$IPA_FILE" ]; then
                echo "Found IPA: $IPA_FILE"
                cp "$IPA_FILE" "build-artifacts/sshbridge_ios.ipa"
              else
                echo "ERROR: IPA not found after build"
                ls -la . | grep -E '\.ipa|build-'
              fi
            fi
          fi
        env:
          EAS_SKIP_AUTO_FINGERPRINT: 1

      - name: Verify build artifacts
        id: detect-artifacts
        run: |
          echo "=== Build Artifacts ==="

          if [ -d "build-artifacts" ]; then
            echo "Contents of build-artifacts/:"
            ls -lh build-artifacts/ 2>/dev/null || echo "Directory exists but is empty"

            ARTIFACT_COUNT=$(ls -1 build-artifacts/ 2>/dev/null | wc -l)
            echo ""
            echo "Total artifacts: $ARTIFACT_COUNT"
          else
            echo "Warning: build-artifacts directory not found"
            mkdir -p build-artifacts
            ARTIFACT_COUNT=0
          fi

          echo "artifact_count=$ARTIFACT_COUNT" >> $GITHUB_OUTPUT

      - name: Submit to App Store
        if: ${{ github.event.inputs.action == 'submit' }}
        run: |
          VERSION=$(node -p "require('./app.json').expo.version")

          if [ "${{ matrix.platform }}" = "android" ]; then
            BUILD_FILE="build-artifacts/sshbridge_android.aab"
            if [ -f "$BUILD_FILE" ]; then
              echo "Using AAB for Play Store submission: $BUILD_FILE"
            else
              echo "ERROR: AAB not found for production submission"
              echo "Play Store requires AAB format for submissions"
              exit 1
            fi
          else
            BUILD_FILE="build-artifacts/sshbridge_ios.ipa"
          fi

          if [ -n "$BUILD_FILE" ] && [ -f "$BUILD_FILE" ]; then
            echo "Submitting file: $BUILD_FILE"
            eas submit -p ${{ matrix.platform }} --profile production --path "$BUILD_FILE"
          else
            echo "No build file found to submit"
            exit 1
          fi

      - name: Upload Android APK
        if: ${{ github.event.inputs.action == 'file' && matrix.platform == 'android' }}
        uses: actions/upload-artifact@v4
        with:
          name: sshbridge_android
          path: build-artifacts/sshbridge_android.apk
          retention-days: 30
          if-no-files-found: error

      - name: Upload iOS IPA
        if: ${{ github.event.inputs.action == 'file' && matrix.platform == 'ios' }}
        uses: actions/upload-artifact@v4
        with:
          name: sshbridge_ios
          path: build-artifacts/sshbridge_ios.ipa
          retention-days: 30
          if-no-files-found: error

      - name: Upload to GitHub Release
        if: ${{ github.event.inputs.action == 'release' }}
        env:
          GH_TOKEN: ${{ secrets.SSHBRIDGE_RELEASE_TOKEN || github.token }}
        run: |
          VERSION=$(node -p "require('./app.json').expo.version")

          echo "Fetching latest release from nghoang1288/SSHBridge-Mobile..."
          MOBILE_RELEASE=$(gh release list --repo nghoang1288/SSHBridge-Mobile --limit 1 --json tagName,name,isLatest -q '.[0]')

          if [ -z "$MOBILE_RELEASE" ]; then
            echo "ERROR: No releases found in nghoang1288/SSHBridge-Mobile"
            exit 1
          fi

          MOBILE_TAG=$(echo "$MOBILE_RELEASE" | jq -r '.tagName')
          MOBILE_NAME=$(echo "$MOBILE_RELEASE" | jq -r '.name')
          MOBILE_URL="https://github.com/nghoang1288/SSHBridge-Mobile/releases/tag/$MOBILE_TAG"

          echo "Latest Mobile release: $MOBILE_NAME ($MOBILE_TAG)"
          echo "Mobile Release URL: $MOBILE_URL"

          if [ "${{ matrix.platform }}" = "android" ]; then
            APK_FILE="build-artifacts/sshbridge_android.apk"
            if [ -f "$APK_FILE" ]; then
              echo "Uploading $APK_FILE to nghoang1288/SSHBridge-Mobile..."
              gh release upload "$MOBILE_TAG" \
                "$APK_FILE" \
                --repo nghoang1288/SSHBridge-Mobile \
                --clobber

              echo "Android APK uploaded to nghoang1288/SSHBridge-Mobile"
            else
              echo "ERROR: Android APK not found: $APK_FILE"
              exit 1
            fi
          elif [ "${{ matrix.platform }}" = "ios" ]; then
            IPA_FILE="build-artifacts/sshbridge_ios.ipa"
            if [ -f "$IPA_FILE" ]; then
              echo "Uploading $IPA_FILE to nghoang1288/SSHBridge-Mobile..."
              gh release upload "$MOBILE_TAG" \
                "$IPA_FILE" \
                --repo nghoang1288/SSHBridge-Mobile \
                --clobber

              echo "iOS IPA uploaded to nghoang1288/SSHBridge-Mobile"
            else
              echo "ERROR: iOS IPA not found: $IPA_FILE"
              exit 1
            fi
          fi

          echo ""
          echo "Build uploaded to release:"
          echo "  - SSHBridge Mobile: $MOBILE_URL"
